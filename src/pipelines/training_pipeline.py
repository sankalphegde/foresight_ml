"""Pipeline stub for future MLflow orchestration."""


def run_training_pipeline() -> None:
    """Placeholder pipeline entrypoint.

    Future implementation template:

    # from src.models.train import train_and_log_model, register_model_from_run
    # from src.models.evaluate import evaluate_model
    #
    # train_result = train_and_log_model()
    # evaluate_model(run_id=train_result["run_id"], model_uri=train_result["model_uri"])
    # register_model_from_run(train_result["run_id"])
    """
    raise NotImplementedError("Training pipeline is intentionally left as a stub.")
